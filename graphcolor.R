# the plot() function also allows labelling using main, xlab, and ylab
plot(1:10, main="My Graph", xlab="The x-axis", ylab="The y axis", col="red", cex=4, pch=16) # nolint

# you can change the size of the points using the cex=2 function as shown above // 1 means default # nolint
# the pch=16 is used to change the shape of the point mapper