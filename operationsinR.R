# addition
print(2+8)

# lets look at the built in operations

print(max(100, 800, 10)) # the max operator outputs the largest number in the list
print(min(100, 800, 10))

print(sqrt(400)) # gets the square root
print(abs(-100.098)) # the absolute number

# the ceiling() and the floor() functions rounds off the number to the nearest integer appropriateky

print(ceiling(22.8))
print(floor(22.8))

# lets talk operators: go to operators.R