# vectors
numbers <- 1:10
print(numbers)

# you can the sequence  you want them // use the seq() function

numbers11 <- seq(from = 0, to = 1000, by = 10) # sys you bstart from 0 to 100 in an interval of 10s
print(numbers11)

